// Development builds only: the web container replaces this file at startup with
// the deployment's runtime config, so index.html reads the marker below.
window.pomodoistBuildEnvironment = 'local';
window.pomodoistRuntimeConfig = window.pomodoistRuntimeConfig || null;
