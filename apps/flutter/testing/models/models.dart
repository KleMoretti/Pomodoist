/// Builders for the domain objects the Flutter tests need.
///
/// Every builder takes the fields a test cares about as named parameters and
/// fills the rest with fixed, valid values, so a test only states what it is
/// actually about.
library;

export 'billing_fixtures.dart';
export 'focus_fixtures.dart';
export 'planning_fixtures.dart';
export 'task_fixtures.dart';
